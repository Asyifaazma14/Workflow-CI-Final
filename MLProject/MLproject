name: CI_Retrain_RF_Baseline

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string}
      target_col: {type: string}
      experiment_name: {type: string}
    command: >
      python modelling.py
      --data_path {data_path}
      --target_col "{target_col}"
      --experiment_name "{experiment_name}"
